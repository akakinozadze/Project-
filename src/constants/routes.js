const home = "/";
const products = "/products";
const cart = "/cart";
const signIn = "/signIn";
const register = "/ register";
const routes = {
  home,
  products,
  cart,
  signIn,
  register,
};

export default routes;
