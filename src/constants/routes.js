const home = "/";
const products = "/products";
const detailsprodact = "/products/:dataId";
const cart = "/cart";

const routes = {
  home,
  products,
  detailsprodact,
  cart,
};

export default routes;
