import React from "react";

const HukFetch = () => {
  const [data, setData] = useState([]);
  const [error, seterror] = useState("");
  const [first, setfirst] = useState(true);

  return <div>HukFetch</div>;
};

export default HukFetch;
